# define nơi allure ghi kết quả (json + attachment) sau mỗi lần chạy test
# define path mặc định target/allure-results vì đây là nơi java mặc định lưu kết quả về

allure.results.directory=target/allure-results